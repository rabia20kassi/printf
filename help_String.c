#include "main.h"

/*** project team laila_rabia ***/
/**
 * help_String - prints the string
 * @args : the first argument
 * Return: integer
 */
int help_no_print(char n)
{
	int n;
	if (n >= 32 && n <= 127)
	{
		return (1);
	}
	return (0);
}
